'use strict'

describe('Simple test', () => {
    // beforeAll(() => {
    //     browser.get(baseUri);
    // });

    it('should have correct title', () => {
        console.log('App test works!!!')
        browser.debug();
    });


    // afterAll(() => {
    //     browser.driver.quit();
    // });
});
